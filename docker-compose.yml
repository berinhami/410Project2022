version: "3.8"

services:

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080

  db:
    build:
      context: ./docker/db
    restart: always
#    env_file: ./.env
    environment:
      POSTGRES_DB: Sudoku2022
      POSTGRES_USER: berinhami
      POSTGRES_PASSWORD: postgres2
    ports:
      - 5432:5432

      # docker build -t Mysudoku
      # docker run --rm -t -p 5000:3000 mysudoku